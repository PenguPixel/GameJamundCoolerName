import RAPIER from "@dimforge/rapier3d-compat";
import { PhysicsCollisionGroup } from "../physics/PhysicsCollisionGroup.js";
import * as THREE from 'three';
import { AssetManager } from "../../core/manager/AssetManager";

export class StaticObjects
{
    // TODO: Objects mit properties:
        // - Mesh (erst greyBox später importiert)
        // - Collider
        // - Animation wenn vorhanden

        // Methode für die Erstellung der Objekte
        // Methode für das Erstellen des Colliders
}